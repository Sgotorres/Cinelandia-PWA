// Renombra este archivo a config.js y coloca tu IP local actual
// Ejemplo: const SERVER_IP = '192.168.X.X';
const SERVER_IP = 'localhost'; 
const PORT = '3000';

export const API_URL = `http://${SERVER_IP}:${PORT}`;